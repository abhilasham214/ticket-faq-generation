# Copy to .env.local for local `vercel dev`, or set these as Vercel project env vars.

# Gemini API key used to draft FAQ text per cluster (https://aistudio.google.com/apikey).
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
